import { KesCurrencyPipe } from './kes-currency.pipe';

describe('KesCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new KesCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
